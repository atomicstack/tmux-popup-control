# demo-svg

animated svg demo recordings embedded by README.md and DEMOS.md on the
main branch. this orphan branch holds only these generated assets and is
force-pushed on re-recording so the main history never grows.

generated by the demo harness (asciinema + svg-term) — see demo/ in the
working tree (a separate repository).
